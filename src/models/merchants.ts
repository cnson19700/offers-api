export class Merchants {
    id: number;
    name: string;
    distance: number;

    constructor(id: number, name: string, distance: number) {
        this.id = id;
        this.name = name;
        this.distance = distance;
    }
}